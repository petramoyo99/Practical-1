# Practical 1 — SQL Fundamentals (Snowflake)

## Overview
This practical builds SQL foundation skills using the dataset:
**retail_sales_dataset.csv**

You will upload the dataset to Snowflake and answer questions using basic SQL concepts such as:
SELECT, DISTINCT, WHERE, ORDER BY, GROUP BY, and Aggregation.

## Dataset Columns
Transaction_ID, Date, Customer_ID, Gender, Age, Product_Category, Quantity, Price_per_Unit, Total_Amount

## Requirements
- One SQL query per question
- Use correct column names
- Use only the SQL concept required
- Proper formatting
- Return only expected output columns

## Submission
Submit a `.sql` file named:
`Practical_1_SQL_Fundamentals.sql`
